SGA Dashboard v3

This is a Vite + React project.

Local run:
npm install
npm run dev

Deploy:
Push files to GitHub repo root and let Vercel auto-deploy.

Important:
- Keep package.json, vite.config.js, index.html, and src/ at the repo root.
- Delete old tailwind.config.js and postcss.config.js if they still exist.
